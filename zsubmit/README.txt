This is a fake z-submit directory for testing. This represents the folder that Zetafax server would monitor to receive sending instructions. This is configured in Zetafax Configuration. 

Note: This assumes you've configured the default Z-SUBMIT in Zetafax Configuration. 